// config.js - Файл конфигурации с API ключом
// ВАЖНО: Этот файл должен быть на вашем сервере и не попадать в систему контроля версий

// Конфигурация API ключей
window.API_KEYS = {
    mistral: 'YOUR_API_KEY_HERE' // Замените на ваш реальный API ключ Mistral
};

// Дополнительные настройки приложения
window.APP_CONFIG = {
    version: '1.0.0',
    maxMessageLength: 1000,
    maxChats: 10,
    theme: 'light'
};